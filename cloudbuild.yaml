steps:
- name: 'gcr.io/cloud-builders/gcloud'
  entrypoint: '/bin/bash'
  args:
    - '-c'
    - |
      gcloud compute ssh instance-1 \
        --project=clouds-labs-403019 \
        --zone=us-central1-a \
        --command="./deploy.sh"
